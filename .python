# Try to only use standard library modules.

alias mkvenv='python -m venv .venv && source .venv/bin/activate'
alias rmvenv='rm -rf .venv'

# stops pip to be used in the global scope
export PIP_REQUIRE_VIRTUALENV=true
